# Compiled binaries
tests/test_softlink
tests/test_nimport
tests/tharvest
tests/tharvest_cli
tests/tharvest_msvc_calibration_refusal
# RFC-0001 §9/§C.2, slice C2: tests/tcompat_report_manifest.nim's own
# runtime binary (built by `runCompatReportManifestChecks` in the nimble
# test task).
tests/tcompat_report_manifest
# RFC-0001 §C.3, slice C4c: tests/tcompat_drift_required.nim's and
# tests/tcompat_drift_refuse_false.nim's own runtime binaries (built by
# `runDriftRequiredChecks` in the nimble test task).
tests/tcompat_drift_required
tests/tcompat_drift_refuse_false
# RFC-0001 §9/§C.2, slice C5: tests/tcompat_degradation.nim's own runtime
# binary (built by `runDegradationChecks` in the nimble test task).
tests/tcompat_degradation
# RFC-0002 §6, slice C3a: tests/tverify_gated_drift.nim's own compiled
# binary (built by `runGatedDriftChecks` in the nimble test task — rmFile'd
# after each run, but the fixture is compiled with real `nim c`, not
# --compileOnly, so it exists on disk between the two invocations).
tests/tverify_gated_drift
# RFC-0002 §5/§6, slice E2: tests/tverify_synthesized_gate.nim's own
# compiled binary — same rationale as tests/tverify_gated_drift above
# (built by `runVersionMacrosGateChecks` in the nimble test task).
tests/tverify_synthesized_gate
# RFC-0001 code-review Lows (#24/R2-5): the tcov_* coverage fixtures are run
# with `nim c -r` (unconditional block in the `test` task), leaving compiled
# binaries next to their sources.
tests/tcov_classify_absence_multi_pair
tests/tcov_harvest_cli_help_ordering
tests/tcov_manifest_bytecap
tests/tcov_manifest_shape_guards
# RFC-0001 slice B5: tests/tharvest.nim's drift-alarm exit-code integration
# test compiles+runs the harvester CLI shim itself (`nim c -r
# tools/harvest/harvester.nim ...`), which builds this binary in place.
tools/harvest/harvester
# RFC-0001 slice B8: the packaged CLI's own `bin` binary — built either by
# `nim c --path:src tools/harvest/softlink_harvest.nim` (the
# tests/tharvest.nim integration test) or `nimble build`/`nimble install`
# inside tools/harvest (see tools/harvest/README.md's package-layout
# section for the local-dependency story via the committed
# tools/harvest/nimble.develop).
tools/harvest/softlink_harvest
tests/libtestlib.so
tests/libtestlib.dylib
tests/testlib.dll
# deriveLibPattern e2e fixtures (built by the test task)
tests/libmagic.so
tests/libmagic.dylib
tests/libmagic.dll
tests/libvern.so.3

# Example binaries
examples/struct_verification
examples/tls_backend

# Blog drafts
docs/

# Nim cache
nimcache/

# RFC-0001 §B.5, slice B6a: per-OS-leg-generated compat manifest fixtures
# — the nimble test task writes these from their `*.tmpl.json` templates
# with THIS CI leg's real target ABI substituted in (see `writeManifestFromTemplate`
# in softlink.nimble); the `.tmpl.json` sources stay tracked.
tests/manifests/*.compat.json
